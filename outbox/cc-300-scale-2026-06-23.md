# 300-Point Quality Scale — 2026-06-23

## Probes (Rule 68)

| # | Probe                                       | Result                                                                        |
|---|---------------------------------------------|--------------------------------------------------------------------------------|
| 1 | Dims 7+10 N/A comments in `scoreProse`      | Confirmed at L414/L419 (journalism-quality.js)                                |
| 2 | `runQualityChain` Layer 3b + final scoreProse | Confirmed at L633 (threshold 175), L640 (retried `scoreProse(text)`), L646 (final) |
| 3 | All 9 call sites                            | Confirmed thresholds 90/130/scoreFloor at each. Game data in scope at L4324 (no), L4436 (yes), L4526 (slate), L5536 (slate), L7103 (id only), L7603 (yes), L9004 (no), L10937 (job), L11025 (job) |
| 4 | `/quality/report` SQL + alerts              | Confirmed: `below_150` / `above_200`; per-type `_alertThreshold(130/140/170)` |
| 5 | **game_id format**                          | Confirmed: `game_brief` rows store `game_id = regular_season_games.id` (matches L7488). night_owl ids look like `night_owl_2026-06-23_mlb_coloradorock_bostonredsox` — these do NOT match D1 ids (they're synthesized from the client). Lookup will succeed for `game_brief`/`mlb_game`-style ids but silently degrade for night_owl. Documented. |
| 6 | `/backfill/brief-scores` handler            | L7696. NULL filter at L7711. UPDATE at L7717. All confirmed.                  |

## What shipped

**`src/journalism-quality.js`:**
- `scoreProse`: Dim 7 (0→25) — 8 pts per team-name hit + 9 for score digit; Dim 10 (0→30) — 10 pts per matchupNote non-stopword hit (cap 30). Ceiling `Math.min(245)` → `Math.min(300)`.
- `runQualityChain`: extract `opts.game` + `opts.matchupNote` at function top. All three internal `scoreProse` calls receive `{sport, game, matchupNote}`. Layer 3b threshold default 175 → 240. Retry prompt now injects exact game data + matchupNote excerpt (so the model has the anchors needed to score Dims 7+10 on retry).

**`src/index.js`:**
- `executeGameBriefBackfill` (L4436): wires `{home, away, homeScore, awayScore, matchupNote}` + threshold 240.
- `/backfill/game-briefs` (L7603): same wiring; pulls from `game.home`/`game.note`.
- Queue consumer game-brief (L10937) + journalism (L11025): wires from `job.{home,away,homeScore,awayScore,matchupNote}` when present; null when slate-shape. Threshold 240.
- Slate cron (L4526) + slate backfill (L5536) + series_preview (L4324): threshold 240 (no game).
- `/journalism/generate` (L9004): unchanged (caller-controlled `scoreFloor`).
- `/archive/brief`: D1 lookup by `game_id` → `regular_season_games` then `postseason_games`. Pulls home/away/scores/note → wires through. Silent degrade when game_id doesn't match (e.g. night_owl ids).
- `/quality/report`: SQL `below_150`/`above_200` → `below_240`/`above_240`. Alerts: per-type `_alertThreshold` helper deleted, single 240 bar. Failure trigger `0.4` → `0.2`. ENRICHMENT_TYPES + golf still excluded.
- `/backfill/brief-scores`: `?rescore=true` switches SELECT to `quality_score IS NOT NULL AND quality_score <= 245`. UPDATE drops the `IS NULL` guard.

## Commit & deploy

- `a3f223b` feat: full 300-point quality scale — implement Dims 7+10, excellence threshold 240 (2 files, +131/−49)
- Deploy: workflow 28062938677 — completed/success.

## Backfill rescore execution

Walked by `?type=<X>` (the global rescore loop doesn't converge because new scores still ≤ 245 keep matching the SELECT):

| Pass         | Type                 | Processed | Notable scores       |
|--------------|----------------------|-----------|----------------------|
| 1 (no type)  | mixed                |        50 | NBA Finals G4: 208   |
| 2            | `night_owl`          |        50 | range 102–190        |
| 3            | `mlb_game`           |        50 | max **269**, 256, 252×2 |
| 4            | `game_recap`         |        50 | max **276**, 253, 250 |
| 5            | `slate`              |        19 | max **247**          |
| 6            | `wc_matchup`         |        50 | range 92–173 (capped by short prose) |
| 7            | `narrative_context`  |        50 | max **300×5** (RSA, ECU, KSA, COL, BIH), several 295+ |
| 8            | `standings_snapshot` |         8 | range 129–158        |
| **Total**    |                      |   **327** | 0 failures           |

## Task 7 verification

**Done conditions:**

1. **Deploy success** — workflow 28062938677, CI green ✓
2. **`/quality/report` uses `below_240` and `above_240` fields** — confirmed in response (every summary row has both keys) ✓
3. **Alert threshold is 240 for all journalism types** — every alert in the response shows `"threshold": 240`, `"alert": "avg_below_240"` ✓
4. **At least one brief scores > 245** — confirmed:
   - `mlb_game` MLB: max 269
   - `game_recap` WNBA: max 250 (in 7-day window); `wnba` lowercase variant 276
   - `narrative_context`: max 300 (five rows hit ceiling)
   - `slate`: max 247
   - `game_recap` FIFA World Cup: max 253
   - 14 of 31 summary rows have `above_240 > 0`
5. **Backfill scoring completes without errors** — 327 rows scored, 0 failures across 8 passes ✓

## Quality distribution after rescore

| Bucket                          | Count                                  |
|---------------------------------|----------------------------------------|
| Types with `above_240 > 0`      | 14 (mlb_game, game_recap MLB/wnba/WNBA/FIFA/fifa lc, slate, narrative_context, …) |
| Types still 100% below 240      | 7 (game_brief MLB/WC/WNBA/golf, wnba_game, night_owl MLB/Baseball MLB/WC) |
| `alert_count`                   | 14 (was 4 on the 245-scale)            |
| `unscored_count`                | 0                                      |

**Alert spike from 4 → 14 is the intended behavior:** the threshold moved to excellence (240), so types that previously sat above the relaxed 130/140/170 thresholds now correctly flag as below the 80% bar. Future generation cycles will use the new Layer 3b retry prompt (with injected game data + matchupNote) to push toward 240+.

## Carry-forwards

1. **Rescore loop convergence.** Without a column tracking "scored on the new code path", the rescore SELECT `quality_score <= 245` keeps matching every freshly-rescored row whose new score happens to be ≤ 245 (i.e. most short-prose types). Walking by `?type=<X>` covers the working set in one pass per type. To fully reach all 100+ rows per type, an `?offset=` param or a `scored_at` timestamp column would be needed — out of scope.
2. **night_owl Dims 7+10 not available.** night_owl `game_id` values (e.g. `g12`, `football:1489397`, `mlb_coloradorock_bostonredsox`) are synthesized client-side and don't match `regular_season_games.id`. The D1 lookup at /archive/brief silently returns null → Dims 7+10 contribute 0 → ceiling effectively still 245 for those briefs. Fixing this needs either a client-side change (use the real D1 id) or a secondary lookup by (date, home, away). Tracked.
3. **Existing low-scoring categories will lift on next generation.** game_brief MLB at 168.4 will hit retry-with-game-context on the next cron tick; expected lift to 200+ as Dims 7+10 fire on regeneration. The 159.6 → 167.8 → 168.4 progression across the last three sessions shows the slope.
4. **wc_matchup capped.** All 57 rows score below 173, reflecting the structurally short reference text. Excluded from alerts via ENRICHMENT_TYPES; no further action needed.
5. **Golf still floor.** game_brief golf 106.4 unchanged; no context builder exists. Excluded from alerts; this is a context-builder gap, not a scoring problem.

## Verify commands

```
probe_relay_route /quality/report
# expect: below_240/above_240 columns; alerts threshold:240
probe_relay_route /backfill/brief-scores?rescore=true&type=<X>&limit=50
# walk by type; loop returns same rows when scores stay ≤ 245
```
