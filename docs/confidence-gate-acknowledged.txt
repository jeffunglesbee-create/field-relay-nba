# Confidence-gate acknowledgment list.
# One outbox filename (relative to repo root) per line. A `#` comment
# above each entry states who/when/why it was reviewed and judged
# non-actionable — required, not optional, so this file stays auditable
# rather than becoming an opaque bypass list. Do NOT add an entry here
# without a real reason; this suppresses a CI check, not decorates one.

# Reviewed 2026-07-08 (chat session): PARTIALLY COMPLETE at 75/100 due
# to an upstream 403 that self-resolved between 2026-07-04 and review;
# endpoint confirmed working live. Process violation stands as a
# historical record but no further action needed.
outbox/cc-archive-catchup-existence-check-fix-2026-07-06.md

# Reviewed 2026-07-08 (chat session): cost-measurement work, 85/100,
# independently reviewed, no actionable defect found.
outbox/cc-drama-score-cost-measurement-2026-07-07.md

# Reviewed 2026-07-08 (chat session): 70/100 was an honest "could not
# conclusively demonstrate cache-hit behavior" result per the CC-CMD's
# own gate, not a quality defect -- superseded by the CC-CMD-2026-07-08-
# afl-kali-kv-cache follow-up (100/100, live cache-hit demonstrated via
# KV), documented in outbox/cc-afl-kali-relayfetch-fix-2026-07-08.md's
# later "TASK 2 Follow-Up" section (not a separate outbox file, despite
# this CC-CMD's own text implying one at the time it was written) --
# which resolved the underlying question this one correctly left open.
outbox/cc-afl-kali-cache-audit-2026-07-08.md

# Reviewed 2026-07-12 (chat session, CC-CMD-completion-field-parity-gate-
# resolution): TASKS 0-3 are complete, real, and live-verified (100%
# genuine work -- see the outbox for the live-invariant fail-then-pass
# proof). TASK 4 (jubilant-bassoon STANDARDS.md Rule 97) was blocked for
# this session by a structural tool-access gap, not a shortcut -- no
# available write path reaches STANDARDS.md (commit_file's
# WRITE_ALLOWLIST is docs/, HANDOFF.md, CODE_MAP.json only; this
# session's GitHub MCP tools are hard-scoped to field-relay-nba only).
# Re-confirmed independently this review, not copied forward. TASK 4 is
# now genuinely resolved: a separate jubilant-bassoon session with real
# write access added Rule 97 (CI-AS-INVARIANT-A) for real, verbatim,
# at the correct number, with its own fail-then-pass-equivalent
# verification (jubilant-bassoon commit b1a1db0cfc7fb0af1a6d42f2de0d5cf8ebf8e7f1,
# "docs: add Rule 97 CI-AS-INVARIANT-A to STANDARDS.md", outbox
# docs/outbox/cc-rule97-ci-as-invariant-2026-07-12.md in that repo) --
# independently re-read and confirmed live at jubilant-bassoon HEAD
# before citing it here, not trusted from the commit message alone.
outbox/cc-completion-field-parity-2026-07-12.md

# Reviewed 2026-07-21 (chat session): 73/100 score is structural, not a
# code-quality failure. The session made zero src/ commits — it was a
# pure investigation of post-deploy-verify.yml's 0-job runs. The score
# deduction was for not retrieving actual log content, which was
# impossible because no jobs ever ran (0-jobs = no logs to retrieve).
# The low score honestly documents a structural ceiling, not a defect.
# The gate fires because unrelated src/ commits from other sessions fall
# within the same 20-commit window; there is no causal relationship.
outbox/cc-session-2026-07-21-investigate-post-deploy-verify-failures.md

# Reviewed 2026-07-21 (chat session): 90/100 score is an infrastructure
# limitation, not a code defect. The session's work was renaming
# post-deploy-verify.yml to a new filename — a .github/workflows/ change
# only, no src/ commits. Sub-95 because workflow_dispatch returned 422
# after the rename (frozen trigger entry, confirmed by this session's
# follow-up investigation). The dispatch failure was the thing being
# investigated, not an oversight. The gate fires due to unrelated src/
# commits in the same 20-commit window; no causal relationship.
outbox/cc-session-2026-07-21-recreate-workflow-new-filename.md
